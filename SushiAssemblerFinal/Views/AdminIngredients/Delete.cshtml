@model SushiAssemblerFinal.Models.Ingredient

@{
    ViewData["Title"] = "Изтриване на съставка";
}

<h1>Изтриване на съставка</h1>

<div class="alert alert-danger">
    Сигурен ли си, че искаш да изтриеш тази съставка?
</div>

<dl class="row">
    <dt class="col-sm-3">Име</dt>
    <dd class="col-sm-9">@Model.Name</dd>

    <dt class="col-sm-3">Тип</dt>
    <dd class="col-sm-9">@Model.Type</dd>

    <dt class="col-sm-3">Цена</dt>
    <dd class="col-sm-9">@Model.Price EUR</dd>
</dl>

<form asp-action="Delete" method="post">
    <input type="hidden" asp-for="Id" />

    <button type="submit" class="btn btn-danger">
        Изтрий
    </button>

    <a asp-controller="AdminIngredients" asp-action="Index" class="btn btn-secondary">
        Отказ
    </a>
</form>